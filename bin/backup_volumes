#!/bin/sh
# stackctl backup-volumes (back up named volumes to the local volumes/ tree)
# Operates on the stack directory of the current working directory.
exec python3 "$(dirname "$0")/stackctl.py" backup-volumes "$@"