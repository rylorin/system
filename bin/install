#!/bin/sh
# stackctl install (load configs, build volumes, deploy the stack)
# Operates on the stack directory of the current working directory.
exec python3 "$(dirname "$0")/stackctl.py" install "$@"